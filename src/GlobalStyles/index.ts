import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  input: {
    width: "100%",
    height: 35,
  },
  textArea: {
    width: "100%",
    height: 60,
  },
  textCenter: {
    textAlign: "center",
  },
});
