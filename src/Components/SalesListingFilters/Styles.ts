import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  toggleContainer: {
    width: "100%",
    flexDirection: "column",
    overflow: "hidden"
  },

  calendarInput: {
    width: "49%",
  },
  input: {
    marginTop: 15,
  },
});
