import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    height: 75,
    marginTop: 30,
    borderRadius: 60,
    borderWidth: 1,
    overflow: "hidden",
  },
  touchable: {
    paddingVertical: 15,
    paddingHorizontal: 20,
    flexDirection: "row",
    alignItems: "center",
  },
  textItem: {
    marginRight: 15,
    maxWidth:"33%",
  },
});
