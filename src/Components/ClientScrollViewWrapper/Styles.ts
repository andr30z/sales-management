import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  editIconContainer: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 5,
    },
    shadowOpacity: 0.36,
    shadowRadius: 6.68,

    elevation: 11,
  },
  whatsappContainer: {
    height: 75,
  },
});
